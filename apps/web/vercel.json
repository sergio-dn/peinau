{
  "framework": "vite",
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @wildlama/shared build && pnpm --filter @wildlama/web build",
  "outputDirectory": "dist",
  "installCommand": "echo 'skip'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
